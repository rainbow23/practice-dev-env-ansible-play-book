---
- name: Install WordPress, MariaDB, Nginx, and PHP-FPM
  hosts: targets
  remote_user: rainbow
  # remote_user: user
  become: yes
  # become_user: rainbow
  roles:
    # - common
    # - mariadb
    # - nginx
    - dev_env
    # - php-fpm
    # - wordpress
